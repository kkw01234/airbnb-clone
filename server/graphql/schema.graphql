type user {
    id:ID!
    user_id: String
    username: String
    email: String,
    password: String,
    super_host: Boolean,
}

type Query{
    user: user,
    users: [user],
    checkUserIdAndPassword(user_id: String!,password: String!): user
}
